// Copyright IBM Corp. 2020, 2024

import * as dotenv from 'dotenv'
import * as debug from 'debug'

debug('stepzen:dotenv')(
  `loading .env from ${process.cwd()}, at transpiler/src/utils/constants.ts`,
)
dotenv.config()

const {STEPZEN_SERVER_URL: ENV_VAR_STEPZEN_SERVER_URL} = process.env

export const EXPERIMENTAL_EXTENSIONS = `
directive @experimental(
  debug: Boolean
  function: String
  mock: String
  setter: String
) on OBJECT | FIELD_DEFINITION
`

export const STEPZEN_SERVER_URL =
  ENV_VAR_STEPZEN_SERVER_URL || 'https://{account}.stepzen.io'

export const ALL_GRAPHQL_FILES = '**/*.graphql'
